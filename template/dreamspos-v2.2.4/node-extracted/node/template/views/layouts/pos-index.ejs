<!DOCTYPE html>
<html lang="en">
  <%- include ('./head') %>

  <body class="pos-page">
    <!-- Main Wrapper -->
    <div class="main-wrapper <%= 
      active_path === routes.pos ? 'pos-five' :
      active_path === routes.pos_3 ? 'pos-two' :
      active_path === routes.pos_4 ? 'pos-three' :
      active_path === routes.pos_5 ? 'pos-three pos-four' :
      '' %>" >

        <!-- Header -->
        <%- include ('./pos-header') %>
        <!-- /Header -->


        <%- include('../pages/'+page_path) %>
    </div>
    <!-- /Main Wrapper -->

    <%- include ('./script') %>

    <%- include ('./theme-settings') %> 

    
  </body>
</html>
